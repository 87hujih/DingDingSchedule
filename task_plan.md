# 任务计划：README 补全与项目总览整理

## 目标
基于当前仓库的真实实现补全根目录 `README.md`，让 GitHub 访问者能够快速理解项目定位、核心能力、技术栈、目录结构、本地运行方式与当前部署链路。

## 当前阶段
阶段 4

## 阶段拆分

### 阶段 1：范围确认与事实采集
- [x] 盘点入口文件、核心模块、配置、命令与部署资产
- [x] 确认项目实际能力边界与外部依赖
- [x] 将关键事实记录到 `findings.md`
- **状态：** 已完成

### 阶段 2：README 结构设计
- [x] 收敛 README 面向的主要读者与信息层级
- [x] 确定需要覆盖的章节与顺序
- [x] 明确哪些内容应写入 README，哪些只保留在专项文档
- **状态：** 已完成

### 阶段 3：文档落盘
- [x] 新建根目录 `README.md`
- [x] 补齐项目简介、功能、技术栈、目录结构、快速开始和部署说明
- [x] 保持内容与现有命令、路径、端口和配置约定一致
- **状态：** 已完成

### 阶段 4：核对与交付
- [x] 回读关键源码和脚本，核对 README 中的事实表述
- [x] 记录本次修改与核验方式
- [x] 准备面向用户的最终总结
- **状态：** 已完成

## 关键问题
1. 当前仓库最适合对外展示的项目定位是什么？
2. 哪些能力已经形成可稳定描述的闭环，哪些仍然应该避免在 README 中过度承诺？
3. README 应该如何在“对外介绍”和“开发接入说明”之间保持平衡？

## 已做决定
| 决定 | 原因 |
|------|------|
| README 以当前代码和脚本为准，不复述已经漂移的旧文档表述 | 避免 GitHub 首页继续传播过期信息 |
| 优先覆盖项目价值、能力边界、快速开始和部署入口 | 这些是仓库访问者最先需要的信息 |
| 以中文为主书写 README，并保留关键英文技术名词 | 与现有仓库文档和用户沟通语言保持一致 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方式 |
|------|----------|----------|
| 暂无 | 0 | 不适用 |

## 备注
- 本次任务只补 README，不改业务实现。
# 任务计划：问题 3 的群聊异步回推编排落地

## 目标
在保持现有群聊异步回推外部行为不变的前提下，把分散在 `pkg/dingtalk/stream.go` 中的并发控制、慢查询提示、Webhook 回复与主动推送 fallback 收束为一个明确的编排器实现，并补上针对关键行为的回归测试。

## 当前阶段
阶段 5

## 阶段拆分

### 阶段 1：现状确认
- [x] 重新核对群聊和私聊链路，确认问题 3 主要落在群聊主路径
- [x] 确认现有群聊已经具备异步处理和主动推送 fallback
- [x] 将关键发现记录到 `findings.md`
- **状态：** 已完成

### 阶段 2：方案定稿
- [x] 比较轻量整理版、编排器版和任务队列版
- [x] 选定“编排器版”作为正式落地方向
- [x] 明确“保持外部行为不变，只重组内部编排职责”的约束
- **状态：** 已完成

### 阶段 3：测试先行
- [x] 为 Webhook 成功路径补充回归测试
- [x] 为 Webhook 失效后的 fallback 路径补充回归测试
- [x] 为并发已满时的繁忙提示补充回归测试
- **状态：** 已完成

### 阶段 4：实现重构
- [x] 新增群聊回复编排器，承载并发控制、慢查询提示和投递策略
- [x] 让 `handleGroupChatAsync` 退化为组装依赖并委派给编排器
- [x] 保持 `SessionWebhook -> 主动推送` 的发送顺序不变
- **状态：** 已完成

### 阶段 5：验证与交付
- [x] 运行定向测试并确认红灯转绿灯
- [x] 运行包级回归，确认 agent 与 dingtalk 相关路径未回归
- [x] 更新任务跟踪与进度记录，准备面向用户的总结
- **状态：** 已完成

## 关键问题
1. 如何在不改变现有群聊可用行为的前提下，减少 `stream.go` 中的内联编排复杂度？
2. 如何把并发控制、慢查询提示、Webhook 回复和 fallback 主动推送这几段逻辑抽成一个清晰的职责边界？
3. 如何用测试锁定“外部行为不变”这个重构约束？

## 已做决定
| 决定 | 原因 |
|------|------|
| 仅重构群聊主路径，不调整私聊语义 | 用户明确表示群聊是主要场景，私聊占比很低 |
| 采用“编排器版”，不直接上持久化任务队列 | 当前需求是提高清晰度和可维护性，而不是引入更重的基础设施 |
| 保留 `SessionWebhook` 优先、主动推送兜底的发送策略 | 这是当前线上可用行为的核心，不应在本次重构中改变 |
| 以回归测试锁定成功、fallback 和繁忙三条关键路径 | 这样可以把“外部行为不变”落成可验证约束，而不是口头约定 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方式 |
|------|----------|----------|
| 定向测试初次执行时报 `undefined: groupChatReplyOrchestrator` | 1 | 先保留测试并实现编排器，使其从红灯转为绿灯 |

## 备注
- 当前计划对应的问题 3 已完成落地，后续若继续扩展可在此基础上拆新的计划文件。
- 若后续补私聊异步补发，应单独建任务，不要混入本次群聊重构范围。

---

# 任务计划：CI/CD 与部署标准化改造规划

## 目标
把当前“本地打包 + SSH 一键部署 + 单条自动部署 workflow”的现状，梳理成一份适合本仓库的标准化实施计划，明确改造阶段、文件边界、触发方式、验收命令和回滚思路，为后续真正实施提供落地蓝图。

## 当前阶段
阶段 4

## 阶段拆分

### 阶段 1：现状确认
- [x] 盘点现有 CI workflow、部署脚本、Docker 资产和部署文档
- [x] 确认当前主流程是 `ONE_CLICK_DEPLOY_GUIDE.md` + `one-click-deploy.sh`
- [x] 确认自动部署链路与手工链路之间的断点
- **状态：** 已完成

### 阶段 2：目标状态收敛
- [x] 明确继续保持“单机 Docker + 单台服务器”架构，不引入更重编排系统
- [x] 明确“正式发布走 GitHub Actions，one-click 脚本作为应急方案”的方向
- [x] 明确“部署镜像而不是部署源码”的标准化目标
- **状态：** 已完成

### 阶段 3：改造任务分解
- [x] 将改造拆成脚本结构整理、CI、生产部署资产、CD、文档收敛和最终验收六个任务
- [x] 为每个任务指定拟创建/修改的文件和验收命令
- [x] 对需要统一的目录、配置和环境变量约定给出推荐值
- **状态：** 已完成

### 阶段 4：计划文档落盘
- [x] 产出仓库内正式计划文档
- [x] 更新任务跟踪和工作记忆文件
- [x] 准备向用户交付执行入口
- **状态：** 已完成

## 关键问题
1. 如何在不引入过重基础设施的前提下，把当前可用但分散的部署方式收敛成标准流程？
2. 如何先解除 `go test ./...` 的结构性阻塞，再补上真正可执行的 CI？
3. 如何让生产部署消费镜像制品而不是服务器现场重建源码？
4. 如何在切换正式流程时保留足够稳妥的手工应急和回滚路径？

## 已做决定
| 决定 | 原因 |
|------|------|
| 继续沿用单机 Docker，不引入 Kubernetes | 当前系统规模和现有运维方式都不需要更重方案 |
| 正式发布链路收敛到 GitHub Actions | 减少“文档、脚本、workflow 各跑一套”的分裂状态 |
| 优先修复 `scripts` 目录结构，再引入 CI | 现有 `go test ./...` 会被多 `main` 冲突阻塞 |
| CD 改为“构建并发布镜像，服务器只拉镜像并启动” | 这是最小但足够规范的制品化部署 |
| `one-click-deploy.sh` 保留为应急入口，不再作为日常正式发布方式 | 这样既保留救火能力，也能避免继续固化非标准主流程 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方式 |
|------|----------|----------|
| 暂无实现类错误；本次任务仅涉及规划与文档落盘 | 0 | 不适用 |

## 备注
- 本计划只负责落地实施蓝图，不直接改动 workflow、脚本和部署资源。
- 后续执行时，应以 `docs/superpowers/plans/2026-03-19-cicd-deployment-standardization-plan.md` 为唯一实施入口。

---

# 任务计划：远程服务器定位当前项目运行位置

## 目标
通过 SSH 登录 `root@106.52.42.194:22`，确认当前仓库对应的在线服务是以什么方式运行，以及实际运行目录、配置位置和可执行入口在哪里。

## 当前阶段
阶段 3

## 阶段拆分

### 阶段 1：远程入口确认
- [x] 建立 SSH 连接并确认当前主机基础信息
- [x] 盘点与本项目相关的进程、容器、systemd 服务和监听端口
- [x] 将初步发现记录到 `findings.md`
- **状态：** 已完成

### 阶段 2：部署路径定位
- [x] 沿着命令行、compose 文件、systemd unit 或进程工作目录追溯部署根目录
- [x] 确认使用的配置文件、镜像名或可执行文件路径
- [x] 区分“源码目录”“运行目录”“数据或日志目录”
- **状态：** 已完成

### 阶段 3：核验与交付
- [x] 用至少两类证据交叉核验结论
- [x] 更新 `tasks/todo.md`、`findings.md` 和 `progress.md`
- [x] 输出面向用户的简明结论，并说明仍未验证的边界
- **状态：** 已完成

## 关键问题
1. 线上当前是以 `systemd`、`docker compose` 还是直接进程方式运行？
2. 当前在线服务对应的项目根目录、配置目录和可执行文件分别在哪里？
3. 是否存在多个候选部署目录，需要进一步区分哪个才是当前生效实例？

## 已做决定
| 决定 | 原因 |
|------|------|
| 先确认运行入口，再追路径 | 直接全盘搜目录噪音太大，容易把旧部署和当前实例混在一起 |
| 以进程 / 容器 / systemd 三条线并行核验 | 这样更快锁定真实在线实例 |
| 结论必须给出具体命令证据 | 避免把“疑似目录”当成当前运行位置 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方式 |
|------|----------|----------|
| 暂无 | 0 | 不适用 |

## 备注
- 本轮任务以远程排查为主，不改业务代码。

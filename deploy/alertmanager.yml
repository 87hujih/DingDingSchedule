global:
  resolve_timeout: 5m

route:
  receiver: dingtalk
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: dingtalk
    webhook_configs:
      - url: 'http://webhook-dingtalk:8060/dingtalk/ops/send'
        send_resolved: true
